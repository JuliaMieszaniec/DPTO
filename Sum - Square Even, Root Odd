import math
def sum_square_even_root_odd(nums):
    array = []
    for num in nums:
        if num % 2 == 0: 
            array.append(num ** 2)
        else:  
            array.append(math.sqrt(num))

    return round(sum(array), 2)
